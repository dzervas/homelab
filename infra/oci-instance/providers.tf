terraform {
  required_providers {
    oci = {
      source = "oracle/oci"
    }
    cloudflare = {
      source = "cloudflare/cloudflare"
    }
    zerotier = {
      source = "zerotier/zerotier"
    }
  }
}
